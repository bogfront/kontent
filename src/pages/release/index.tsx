import React from 'react';
import {withLayout} from "@/layout/Layout";

function Release() {

  return (
      <>
          <h1>Страница релиза</h1>
      </>
  );
}

export default withLayout(Release);
